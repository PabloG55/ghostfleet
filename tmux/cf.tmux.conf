# claude-fleet tmux config — used for the per-zellij-session `cf-*` sockets.
# tmux is the hidden substrate that keeps each Claude session alive in the
# background; the claude-fleet grid is the front-end. This runs NESTED inside a
# zellij pane, so keep bindings clear of zellij's (C-g/p/t/n/h/s/o/q + Alt-n/f).

# Prefix on C-a (zellij doesn't grab it). C-a C-a sends a literal C-a to the app.
set -g prefix C-a
unbind C-b
bind C-a send-prefix

# Step back one level = detach the client (the session keeps running). From
# master that's back to Projects; from a worker it's back to the sessions grid.
#   `      one-key back (no prefix)        <-- fastest
#   C-a g  (mnemonic: Grid)  /  C-a d      <-- fallback
# NOTE: the no-prefix backtick means you can't type a literal ` inside a session
# while attached. Comment the next line out (and use C-a g) if that bothers you.
bind -n '`' detach-client
bind g detach-client
bind d detach-client

# Jump to the sessions grid. Drops a "<socket>.goto" marker that the claude-fleet
# loop reads on detach (see enter_master in bin/claude-fleet), then detaches. (In a
# worker it just acts like a plain back.)
#   C-s    one chord, no prefix        <-- fastest
#   C-a s  (fallback / mnemonic)
# C-s only reaches tmux because the fleet zellij layout unbinds zellij's own Ctrl-s
# (scroll mode) — see layouts/fleet.kdl. If you launch outside that layout, use C-a s.
bind -n C-s run-shell 'printf grid > "#{socket_path}.goto"' \; detach-client
bind s run-shell 'printf grid > "#{socket_path}.goto"' \; detach-client

# C-p (no prefix) -> jump all the way up to PROJECTS from anywhere: master or an
# active chat. Drops a "projects" marker the claude-fleet loop reads on detach
# (enter_master + grid_loop both check it), then detaches — the session keeps running.
# NB: like the backtick binding, this takes C-p away from Claude inside a session.
bind -n C-p run-shell 'printf projects > "#{socket_path}.goto"' \; detach-client
bind p run-shell 'printf projects > "#{socket_path}.goto"' \; detach-client

# INSTA-JUMP, from master or any chat (the numbers are printed on the cards):
#   C-f <p> <s>     -> project p, SESSION s        e.g. C-f 1 1
#   C-f <p> Enter   -> project p's MASTER          (m works too)
#   C-f <p> s       -> project p's session GRID
#   C-f s <p>       -> same grid, short form
#   C-f p           -> the Projects screen
# The project digit can't fire on its own: once "C-f 1 1" must mean project 1 /
# session 1, pressing 1 has to WAIT for what follows — so the lead takes an explicit
# Enter. Each step is a one-shot tmux key table, so nothing reaches Claude's input;
# Escape backs out. Prefixed fallback: C-a f … (identical).
bind -n C-f switch-client -T cfjump
bind    f   switch-client -T cfjump
bind -T cfjump 1 switch-client -T cfjump1
bind -T cfjump 2 switch-client -T cfjump2
bind -T cfjump 3 switch-client -T cfjump3
bind -T cfjump 4 switch-client -T cfjump4
bind -T cfjump 5 switch-client -T cfjump5
bind -T cfjump 6 switch-client -T cfjump6
bind -T cfjump 7 switch-client -T cfjump7
bind -T cfjump 8 switch-client -T cfjump8
bind -T cfjump 9 switch-client -T cfjump9
bind -T cfjump p run-shell 'printf projects > "#{socket_path}.goto"' \; detach-client
bind -T cfjump s switch-client -T cfjumpg
bind -T cfjump Escape switch-client -T root
bind -T cfjumpg 1 run-shell 'printf jump:1 > "#{socket_path}.goto"' \; detach-client
bind -T cfjumpg 2 run-shell 'printf jump:2 > "#{socket_path}.goto"' \; detach-client
bind -T cfjumpg 3 run-shell 'printf jump:3 > "#{socket_path}.goto"' \; detach-client
bind -T cfjumpg 4 run-shell 'printf jump:4 > "#{socket_path}.goto"' \; detach-client
bind -T cfjumpg 5 run-shell 'printf jump:5 > "#{socket_path}.goto"' \; detach-client
bind -T cfjumpg 6 run-shell 'printf jump:6 > "#{socket_path}.goto"' \; detach-client
bind -T cfjumpg 7 run-shell 'printf jump:7 > "#{socket_path}.goto"' \; detach-client
bind -T cfjumpg 8 run-shell 'printf jump:8 > "#{socket_path}.goto"' \; detach-client
bind -T cfjumpg 9 run-shell 'printf jump:9 > "#{socket_path}.goto"' \; detach-client
bind -T cfjumpg Escape switch-client -T root

# project sub-tables: after C-f <p>, pick a session digit / Enter (master) / s (grid)
bind -T cfjump1 1 run-shell 'printf jumps:1:1 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump1 2 run-shell 'printf jumps:1:2 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump1 3 run-shell 'printf jumps:1:3 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump1 4 run-shell 'printf jumps:1:4 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump1 5 run-shell 'printf jumps:1:5 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump1 6 run-shell 'printf jumps:1:6 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump1 7 run-shell 'printf jumps:1:7 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump1 8 run-shell 'printf jumps:1:8 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump1 9 run-shell 'printf jumps:1:9 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump1 Enter run-shell 'printf jumpm:1 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump1 m     run-shell 'printf jumpm:1 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump1 s     run-shell 'printf jump:1 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump1 Escape switch-client -T root

bind -T cfjump2 1 run-shell 'printf jumps:2:1 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump2 2 run-shell 'printf jumps:2:2 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump2 3 run-shell 'printf jumps:2:3 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump2 4 run-shell 'printf jumps:2:4 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump2 5 run-shell 'printf jumps:2:5 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump2 6 run-shell 'printf jumps:2:6 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump2 7 run-shell 'printf jumps:2:7 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump2 8 run-shell 'printf jumps:2:8 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump2 9 run-shell 'printf jumps:2:9 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump2 Enter run-shell 'printf jumpm:2 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump2 m     run-shell 'printf jumpm:2 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump2 s     run-shell 'printf jump:2 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump2 Escape switch-client -T root

bind -T cfjump3 1 run-shell 'printf jumps:3:1 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump3 2 run-shell 'printf jumps:3:2 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump3 3 run-shell 'printf jumps:3:3 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump3 4 run-shell 'printf jumps:3:4 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump3 5 run-shell 'printf jumps:3:5 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump3 6 run-shell 'printf jumps:3:6 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump3 7 run-shell 'printf jumps:3:7 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump3 8 run-shell 'printf jumps:3:8 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump3 9 run-shell 'printf jumps:3:9 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump3 Enter run-shell 'printf jumpm:3 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump3 m     run-shell 'printf jumpm:3 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump3 s     run-shell 'printf jump:3 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump3 Escape switch-client -T root

bind -T cfjump4 1 run-shell 'printf jumps:4:1 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump4 2 run-shell 'printf jumps:4:2 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump4 3 run-shell 'printf jumps:4:3 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump4 4 run-shell 'printf jumps:4:4 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump4 5 run-shell 'printf jumps:4:5 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump4 6 run-shell 'printf jumps:4:6 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump4 7 run-shell 'printf jumps:4:7 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump4 8 run-shell 'printf jumps:4:8 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump4 9 run-shell 'printf jumps:4:9 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump4 Enter run-shell 'printf jumpm:4 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump4 m     run-shell 'printf jumpm:4 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump4 s     run-shell 'printf jump:4 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump4 Escape switch-client -T root

bind -T cfjump5 1 run-shell 'printf jumps:5:1 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump5 2 run-shell 'printf jumps:5:2 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump5 3 run-shell 'printf jumps:5:3 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump5 4 run-shell 'printf jumps:5:4 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump5 5 run-shell 'printf jumps:5:5 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump5 6 run-shell 'printf jumps:5:6 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump5 7 run-shell 'printf jumps:5:7 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump5 8 run-shell 'printf jumps:5:8 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump5 9 run-shell 'printf jumps:5:9 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump5 Enter run-shell 'printf jumpm:5 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump5 m     run-shell 'printf jumpm:5 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump5 s     run-shell 'printf jump:5 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump5 Escape switch-client -T root

bind -T cfjump6 1 run-shell 'printf jumps:6:1 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump6 2 run-shell 'printf jumps:6:2 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump6 3 run-shell 'printf jumps:6:3 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump6 4 run-shell 'printf jumps:6:4 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump6 5 run-shell 'printf jumps:6:5 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump6 6 run-shell 'printf jumps:6:6 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump6 7 run-shell 'printf jumps:6:7 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump6 8 run-shell 'printf jumps:6:8 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump6 9 run-shell 'printf jumps:6:9 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump6 Enter run-shell 'printf jumpm:6 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump6 m     run-shell 'printf jumpm:6 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump6 s     run-shell 'printf jump:6 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump6 Escape switch-client -T root

bind -T cfjump7 1 run-shell 'printf jumps:7:1 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump7 2 run-shell 'printf jumps:7:2 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump7 3 run-shell 'printf jumps:7:3 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump7 4 run-shell 'printf jumps:7:4 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump7 5 run-shell 'printf jumps:7:5 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump7 6 run-shell 'printf jumps:7:6 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump7 7 run-shell 'printf jumps:7:7 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump7 8 run-shell 'printf jumps:7:8 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump7 9 run-shell 'printf jumps:7:9 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump7 Enter run-shell 'printf jumpm:7 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump7 m     run-shell 'printf jumpm:7 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump7 s     run-shell 'printf jump:7 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump7 Escape switch-client -T root

bind -T cfjump8 1 run-shell 'printf jumps:8:1 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump8 2 run-shell 'printf jumps:8:2 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump8 3 run-shell 'printf jumps:8:3 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump8 4 run-shell 'printf jumps:8:4 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump8 5 run-shell 'printf jumps:8:5 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump8 6 run-shell 'printf jumps:8:6 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump8 7 run-shell 'printf jumps:8:7 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump8 8 run-shell 'printf jumps:8:8 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump8 9 run-shell 'printf jumps:8:9 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump8 Enter run-shell 'printf jumpm:8 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump8 m     run-shell 'printf jumpm:8 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump8 s     run-shell 'printf jump:8 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump8 Escape switch-client -T root

bind -T cfjump9 1 run-shell 'printf jumps:9:1 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump9 2 run-shell 'printf jumps:9:2 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump9 3 run-shell 'printf jumps:9:3 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump9 4 run-shell 'printf jumps:9:4 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump9 5 run-shell 'printf jumps:9:5 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump9 6 run-shell 'printf jumps:9:6 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump9 7 run-shell 'printf jumps:9:7 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump9 8 run-shell 'printf jumps:9:8 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump9 9 run-shell 'printf jumps:9:9 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump9 Enter run-shell 'printf jumpm:9 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump9 m     run-shell 'printf jumpm:9 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump9 s     run-shell 'printf jump:9 > "#{socket_path}.goto"' \; detach-client
bind -T cfjump9 Escape switch-client -T root

# Always-visible reminder of how to get back to the grid (this is the one thing
# that isn't obvious once you're inside a session). Renders at the bottom of the
# pane, above zellij's own bar.
set -g status on
set -g status-interval 5
set -g status-justify left
set -g status-style "bg=colour236,fg=colour252"
set -g status-left "#[bg=colour148,fg=colour16,bold] ● #S #[bg=colour236,fg=colour148,nobold]▏"
set -g status-left-length 60
# master shows the Ctrl-s / ` shortcuts; workers show the plain back hint.
# (Styles use one attribute per #[...] — a comma inside #[...] would be parsed as
# a #{?...} argument separator and split the conditional.)
set -g status-right "#{?#{==:#S,master},#[fg=colour252]#[bold]Ctrl-s#[fg=colour246]#[nobold] sessions   #[fg=colour252]#[bold]\`#[fg=colour246]#[nobold] back to #[fg=colour148]#[bold]Projects  ,#[fg=colour246]press #[fg=colour252]#[bold]\`#[fg=colour246]#[nobold] (or #[fg=colour252]#[bold]Ctrl-a g#[fg=colour246]#[nobold]) to go back to the #[fg=colour148]#[bold]fleet grid  }"
set -g status-right-length 80
set -g window-status-format ""
set -g window-status-current-format ""

# Behave well nested + with Claude/vim.
set -g mouse on
set -sg escape-time 10
set -g focus-events on
set -g default-terminal "tmux-256color"
set -as terminal-features ",*:RGB"     # truecolor passthrough (zellij -> tmux -> claude)
setw -g aggressive-resize on

# Keep sessions when you detach; only close a session when its process exits.
set -g destroy-unattached off
set -g remain-on-exit off
